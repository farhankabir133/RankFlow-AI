name: Verification Gate

rules:
  - Every feature must pass TypeScript compilation
  - Every feature must have Vitest tests
  - No merge conflicts allowed in final code
  - No feature is complete without verification steps
  - AI must re-check architecture before finishing task
